<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.learnmaoriapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LearnMaoriApp">
        <activity
            android:name=".ColorsActivity"
            android:label="Colors"
            android:exported="false"
        android:theme="@style/ColorsActivityTheme" />
        <activity
            android:name=".FamilyActivity"
            android:label="Family"
            android:exported="false"
        android:theme="@style/FamilyActivityTheme" />
        <activity
            android:name=".NumbersActivity"
            android:exported="false"
            android:label="Numbers"
            android:theme="@style/NumberActivityTheme" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>